import { createContext } from "react";


// step 1, import createContext from react and createContext() - equivalent of creating store
export const ProductContext = createContext();
// the above includes a ProductContext.provider brought in.